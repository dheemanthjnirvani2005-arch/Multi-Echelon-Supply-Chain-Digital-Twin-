# mosquitto/config/mosquitto.conf

# Allow connections without username/password (OK for local dev)
allow_anonymous true

# Standard MQTT port
listener 1883

# WebSocket port — lets browsers connect directly
listener 9001
protocol websockets

# Logging
log_dest stdout
log_type all
